<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>SolutionEncryptor</RootNamespace>
    <!-- 与主程序共享同一份加密源码（link 编译），杜绝两侧加解密实现漂移（同 VmBridgeProtocol 模式） -->
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="..\..\Common\VmBridge\SolutionProtector.cs" Link="SolutionProtector.cs" />
  </ItemGroup>

</Project>
